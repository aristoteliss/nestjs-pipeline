{
  "name": "nestjs-pipeline-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "NestJS CQRS pipeline behavior monorepo",
  "license": "SEE LICENSE IN LICENSE",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "rebuild": "pnpm -r clean && pnpm -r build",
    "publish:all": "pnpm -r publish --access public",
    "check": "biome check .",
    "format": "biome check --write .",
    "biome:lint": "biome lint --write .",
    "verify:package-licenses": "node scripts/verify-package-licenses.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.10",
    "@types/node": "^26.2.0",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "overrides": {
    "@nestjs/core": "11.2.1",
    "@nestjs/common": "11.2.1",
    "@nestjs/cqrs": "11.0.3"
  }
}
