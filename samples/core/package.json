{
  "name": "@nestjs-pipeline/sample-core",
  "version": "0.0.1",
  "private": true,
  "description": "Shared sample domain package for nestjs-pipeline examples",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@nestjs-pipeline/core": "workspace:*"
  }
}